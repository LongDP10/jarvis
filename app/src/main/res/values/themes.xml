<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A bare platform theme is enough: every pixel of chrome is drawn by Compose.
      The window background is set to the JARVIS surface colour so there is no
      white flash between process start and the first frame.
    -->
    <style name="Theme.Jarvis" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/jarvis_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
