<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing here should ever leave the device. The encrypted key store in
  particular is keystore-bound and would be useless (and dangerous) in a
  transfer, so both cloud backup and device transfer are refused outright.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
